meta {
  name: Signup
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/v1/accounts
  body: json
  auth: none
}

body:json {
  {
    "fullName": "QA Missionario",
    "username": "qa_missionario_01",
    "phoneNumber": "+5511911111111",
    "gender": "MALE",
    "email": "{{test_email}}",
    "password": "{{test_password}}",
    "passwordConfirmation": "{{test_password}}"
  }
}

docs {
  Cria um novo usuário e emite o par de tokens de autenticação no corpo da
  resposta (access token + refresh token).
}
