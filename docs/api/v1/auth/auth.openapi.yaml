paths:
  /auth/login:
    post:
      operationId: authLogin
      tags: [auth]
      summary: Login
      description: >-
        Autentica com login (email) e senha. Emite um access token JWT (15 min) e um
        refresh token opaco. Ambos sempre voltam no corpo da resposta — não há
        entrega via cookie nesta API, já que o consumidor web é um BFF
        (Next.js), não um browser. Ver ADR-0023, seção "Entrega de Tokens".
      security: []
      parameters:
        - $ref: '#/components/parameters/ClientType'
        - $ref: '#/components/parameters/DeviceName'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Login realizado com sucesso.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
        '400':
          description: Credenciais inválidas.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Erro de validação do corpo da requisição.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationErrorResponse'
        '423':
          description: >-
            Conta temporariamente bloqueada por excesso de tentativas de
            login falhadas. A mensagem informa quantos segundos faltam para
            o desbloqueio.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/forgot-password:
    post:
      operationId: authForgotPassword
      tags: [auth]
      summary: Solicitar redefinição de senha
      description: >-
        Envia um email com link de redefinição de senha, se o login informado
        corresponder a um usuário cadastrado. Sempre responde 200 com a
        mesma mensagem genérica, exista ou não o email — evita user
        enumeration (não revela se um endereço está cadastrado). O link
        expira em 60 minutos.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ForgotPasswordRequest'
      responses:
        '200':
          description: Solicitação processada (independente de o email existir).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageResponse'
        '422':
          description: Erro de validação do corpo da requisição.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationErrorResponse'

  /auth/reset-password:
    patch:
      operationId: authResetPassword
      tags: [auth]
      summary: Redefinir senha
      description: >-
        Troca a senha mediante apresentação do token recebido por email.
        Revoga todas as sessões ativas e reseta o bloqueio de força bruta —
        apresentar o token é prova de controle sobre o email cadastrado. Ver
        ADR-0023.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResetPasswordRequest'
      responses:
        '200':
          description: Senha redefinida com sucesso.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageResponse'
        '401':
          description: Token inexistente ou expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Erro de validação do corpo da requisição.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationErrorResponse'

  /auth/refresh:
    post:
      operationId: authRefresh
      tags: [auth]
      summary: Renovação de access token
      description: >-
        Rotaciona o refresh token e emite um novo access token. O refresh
        token vai sempre no corpo — não há mais fallback via cookie.
        Reutilizar um refresh token já rotacionado invalida toda a família de
        tokens da sessão (detecção de roubo — ver ADR-0023). O TTL do novo
        refresh token é recalculado a partir de agora (sliding window).
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshRequest'
      responses:
        '200':
          description: Novo par de tokens emitido.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
        '401':
          description: >-
            Refresh token ausente, inválido, expirado ou já revogado
            (reuso detectado — toda a família foi invalidada).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/logout:
    delete:
      operationId: authLogout
      tags: [auth]
      summary: Logout (dispositivo atual)
      description: >-
        Revoga a família de refresh token enviada no corpo — outras sessões
        do mesmo usuário não são afetadas. Para logout em todos os
        dispositivos, ver `DELETE /auth/sessions`.


        Não exige access token válido (RFC 7009 — endpoints de revogação não
        devem depender de credencial ainda ativa): a fonte primária de
        revogação é o refresh token, não o access token. Se o header
        `Authorization` vier com um access token ainda válido, seu `jti`
        também é bloqueado no DragonflyDB como defesa extra; ausente/expirado/
        inválido não impede o logout. Resposta sempre 200, mesmo com refresh
        token desconhecido ou já revogado — idempotente, sem vazar validade
        do token. Ver ADR-0023.
      security:
        - bearerAuth: []
        - {}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshRequest'
      responses:
        '200':
          description: Logout realizado com sucesso (idempotente).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageResponse'
        '422':
          description: Corpo da requisição inválido (refreshToken ausente).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/sessions:
    get:
      operationId: authSessionsIndex
      tags: [auth]
      summary: Listar sessões ativas
      description: >-
        Lista as sessões (dispositivos) ativas do usuário autenticado — uma
        entrada por família de refresh token, ordenadas pela mais usada
        recentemente. `id` é o `family_id`, usado para revogar uma sessão
        específica em `DELETE /auth/sessions/{familyId}`.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Lista de sessões ativas.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/SessionSummary'
        '401':
          description: Access token ausente, inválido ou revogado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    delete:
      operationId: authSessionsDestroyAll
      tags: [auth]
      summary: Logout em todos os dispositivos
      description: >-
        Encerra todas as sessões do usuário: incrementa o `auth_version`
        (invalida instantaneamente todos os access tokens ativos, de todos os
        dispositivos) e revoga todos os refresh tokens. Ação explícita,
        distinta do logout comum. Ver ADR-0023.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Todas as sessões foram encerradas.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageResponse'
        '401':
          description: Access token ausente, inválido ou revogado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/sessions/{familyId}:
    delete:
      operationId: authSessionsDestroy
      tags: [auth]
      summary: Revogar sessão remota
      description: >-
        Revoga remotamente uma sessão/dispositivo específico, identificado
        pelo `id` retornado em `GET /auth/sessions`. O access token daquela
        sessão é rejeitado já na próxima requisição — via blocklist de
        família no DragonflyDB — sem esperar sua expiração natural de 15
        minutos. Ver ADR-0023.
      security:
        - bearerAuth: []
      parameters:
        - name: familyId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Sessão encerrada.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageResponse'
        '401':
          description: Access token ausente, inválido ou revogado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Sessão não encontrada, ou pertence a outro usuário.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: familyId ausente ou não é um UUID válido.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

components:
  parameters:
    ClientType:
      name: x-client-type
      in: header
      required: false
      description: >-
        `mobile` ou ausente (assume `web`). Não determina mais onde os tokens
        são entregues (sempre no corpo, em ambos os casos) — apenas a política
        de TTL do refresh token (sliding window mais longa para mobile) e o
        `deviceType` exibido em `GET /auth/sessions`.
      schema:
        type: string
        enum: [mobile]

    DeviceName:
      name: x-device-name
      in: header
      required: false
      description: >-
        Nome legível do dispositivo/cliente (ex: "iPhone 15", "Chrome no
        macOS"), exibido apenas em `GET /auth/sessions`.
      schema:
        type: string
        maxLength: 120

  schemas:
    LoginRequest:
      type: object
      required: [login, password]
      properties:
        login:
          type: string
          format: email
          maxLength: 254
        password:
          type: string
          minLength: 8
          maxLength: 128

    RefreshRequest:
      type: object
      required: [refreshToken]
      properties:
        refreshToken:
          type: string

    ForgotPasswordRequest:
      type: object
      required: [login]
      properties:
        login:
          type: string
          format: email
          maxLength: 254

    ResetPasswordRequest:
      type: object
      required: [token, newPassword, newPasswordConfirmation]
      properties:
        token:
          type: string
        newPassword:
          type: string
          minLength: 8
          maxLength: 32
        newPasswordConfirmation:
          type: string

    LoginResponse:
      type: object
      properties:
        data:
          type: object
          properties:
            user:
              $ref: '#/components/schemas/UserSummary'
            accessToken:
              type: string
              description: >-
                JWT — payload contém sub, jti, role, auth_version, fid
                (family_id do refresh token). TTL de 15 min.
            refreshToken:
              type: string
              description: Token opaco — sempre presente, para qualquer cliente.

    UserSummary:
      type: object
      properties:
        id:
          type: string
          format: uuid
        fullName:
          type: string
        email:
          type: string
          format: email
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    SessionSummary:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: family_id do refresh token — usado para revogar esta sessão.
        deviceType:
          type: string
          enum: [web, mobile]
        deviceName:
          type: [string, 'null']
        ipAddress:
          type: [string, 'null']
        lastUsedAt:
          type: [string, 'null']
          format: date-time
        createdAt:
          type: string
          format: date-time
        current:
          type: boolean
          description: Se é a sessão do access token usado nesta requisição.

    MessageResponse:
      type: object
      properties:
        message:
          type: string

    ErrorResponse:
      type: object
      properties:
        message:
          type: string

    ValidationErrorResponse:
      type: object
      properties:
        errors:
          type: array
          items:
            type: object
            properties:
              message:
                type: string
              rule:
                type: string
              field:
                type: string
